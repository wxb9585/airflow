# GitHub Actions workflow disabled in fork.
# Restore from upstream if CI is needed again.
---
name: "Disabled: finalize-tests.yml"
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in this fork."
